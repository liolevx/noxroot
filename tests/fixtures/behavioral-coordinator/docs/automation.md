# Existing automation

The automation creates and owns Git branches and worktrees, then commits or merges completed work.
It invokes coding workers that implement repository code changes. It selects and runs the repository
verification commands after implementation. It invokes an independent reviewer before completing the
change.
