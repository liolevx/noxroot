# Repository instructions

Read the project knowledge index before changing an application surface. Keep page behavior in the
route, reusable UI in components, and persistence logic in lib. Run the approved checks that match
the changed paths.
